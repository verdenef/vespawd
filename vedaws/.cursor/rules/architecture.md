Vedaws Architecture Rule

When working inside the design/ directory:

- Never invent architecture.
- Never expand TODOs unless explicitly instructed.
- Never redesign existing documents.
- Treat design documents as authoritative specifications.
- Suggest inconsistencies instead of silently fixing them.
- Ask for clarification when implementation conflicts with the design.

Implementation follows architecture, never the reverse.